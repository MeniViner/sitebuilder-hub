services:
  mongo:
    image: mongo:7
    container_name: sitebuilder-hub-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - sitebuilder_hub_mongo:/data/db

volumes:
  sitebuilder_hub_mongo:
